language: r
cache: packages
sudo: required
warnings_are_errors: true

addons:
  apt:
    sources: ['ubuntu-toolchain-r-test']
    packages: ['g++-6']

env:
  - COMPILER=g++-6

r:
  - devel
  - oldrel
  - release

before_install:
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 100
  - Rscript -e 'install.packages("knitr")'
  - cd Rborist/Package/
  - chmod u+x Rborist.travis.sh
  - ./Rborist.travis.sh

install:
  - tar -xzvf Rborist_*.*-*.tar.gz
  #- Rscript -e 'install.packages("rmarkdown")'
  #- Rscript -e 'install.packages("roxygen2")'
  #- Rscript -e 'install.packages("devtools")'
  - Rscript -e 'remotes::install_deps("Rborist", dependencies = T)'
script:
  - Rscript -e 'remotes::check("Rborist", manual = FALSE)'
  
notifications:
  email:
    on_success: change
    on_failure: change

r_github_packages:
  - jimhester/covr

# This can be enabled if you want code coverage
#after_success:
#  - Rscript -e 'install.packages("covr"); covr::coveralls("Rborist")'
